spring:
  application:
    name: betting-service
  grpc:
    server:
      port: 9092
      health:
        enabled: true

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: always
