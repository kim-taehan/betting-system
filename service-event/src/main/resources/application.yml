spring:
  application:
    name: event-service
  grpc:
    server:
      port: 9091

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: always
